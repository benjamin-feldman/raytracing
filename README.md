based off [_Ray Tracing in One Weekend_](https://raytracing.github.io/books/RayTracingInOneWeekend.html)

# use
create image from the setup in `main.cpp`: `g++ -o bin/main src/main.cpp; ./bin/main >> img.ppm`
